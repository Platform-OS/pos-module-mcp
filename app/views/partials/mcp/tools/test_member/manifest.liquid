{%- comment -%}
  Tool manifest — test_member (TEST FIXTURE, app-owned; gated by MCP_ENABLE_TEST_TOOLS).
  Visibility probe for the MEMBER tier: policy test_principal_member → only a principal
  with an active mcp_access role (user|admin). The coverage suite asserts it is
  visible/callable by operator and member, but NOT by outsider or anon.
{%- endcomment -%}
{% liquid
  assign m = '{}' | parse_json
  hash_assign m['name'] = 'test_member'
  hash_assign m['version'] = '1.0.0'
  hash_assign m['description'] = 'TEST FIXTURE (not a production tool): member-tier visibility probe; returns a constant marker.'
  assign schema = '{"type":"object","additionalProperties":false,"properties":{}}' | parse_json
  hash_assign m['input_schema'] = schema
  hash_assign m['mutating'] = false
  hash_assign m['authorization_policy'] = 'test_principal_member'
  hash_assign m['requires_approval'] = false
  hash_assign m['idempotent'] = false
  hash_assign m['handler'] = 'mcp/tools/test_member/call'
  return m
%}
