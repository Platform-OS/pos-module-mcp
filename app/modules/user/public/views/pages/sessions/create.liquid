---
method: post
slug: sessions
layout: 'modules/community/blank'
---

{% liquid
  function current_profile = 'modules/user/helpers/current_profile'

  # platformos-check-disable DeprecatedTag
  include 'modules/user/helpers/can_do_or_redirect', requester: current_profile, do: 'sessions.create', return_url: '/', entity: null, access_callback: null
  # platformos-check-enable DeprecatedTag

  function res = 'modules/user/commands/session/create', email: context.params.email, password: context.params.password, hook_params: context.params, validate_password: true, skip_otp: null

  if res.valid
    # platformos-check-disable DeprecatedTag
    include 'modules/core/helpers/redirect_to', url: null, error: null, info: null, notice: null, object: null, default: null, format: null
    # platformos-check-enable DeprecatedTag
  else
    render 'modules/user/sessions/new', errors: res.errors, values: null
  endif
%}
