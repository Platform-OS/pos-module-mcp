query page_by_slug($slug: String!) {
  admin_pages(
    per_page: 1
    filter: {
      slug: { value: $slug }
    }
  ) {
    results {
      id
      slug
      format
      content
      content_updated_at
      metadata
    }
  }
}
