{% liquid
  assign img_url = profile.avatar.photo.versions[version]
  unless img_url
    # platformos-check-disable MissingAsset
    assign img_url = 'images/user.svg' | asset_url
    # platformos-check-enable MissingAsset
  endunless

  return img_url
%}
