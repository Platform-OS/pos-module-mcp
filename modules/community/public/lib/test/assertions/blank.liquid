{% liquid
  hash_assign contract['total'] = contract['total'] | plus: 1
  unless object[field_name] == blank
    # platformos-check-disable MissingPartial
    function contract = 'test/register_error_f', contract: contract, field_name: field_name, key: 'test.should.be_blank'
    # platformos-check-enable MissingPartial
  endunless
  return contract
%}
