{% liquid
  hash_assign contract['total'] = contract['total'] | plus: 1
  if object[field_name] != blank
    # platformos-check-disable MissingPartial
    function contract = 'test/register_error_f', contract: contract, field_name: field_name, key: 'test.should.not.be_blank'
    # platformos-check-enable MissingPartial
  endif
  return contract
%}
