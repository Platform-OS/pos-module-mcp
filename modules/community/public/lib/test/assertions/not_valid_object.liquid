{% liquid
  hash_assign contract['total'] = contract['total'] | plus: 1
  if object.valid == true
    # platformos-check-disable MissingPartial
    function contract = 'test/register_error_f', contract: contract, field_name: field_name, key: 'test.should.not.be_valid'
    # platformos-check-enable MissingPartial
  endif
  return contract
%}
