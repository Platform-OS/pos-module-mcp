---
slug: groups/:id/set-admin
method: post
---
{% liquid
  function current_profile = 'modules/community/helpers/current_profile', followships: true, membership: true
  function group = 'modules/community/queries/groups/find', id: context.params.id, with_owner: false, current_profile: current_profile
  # platformos-check-disable DeprecatedTag
  include 'modules/user/helpers/can_do_or_unauthorized', entity: group, do: 'set_admin', requester: current_profile, access_callback: 'modules/community/can/groups', anonymous_return_to: null, redirect_anonymous_to_login: false, forbidden_partial: null
  # platformos-check-enable DeprecatedTag

  assign now = 'now' | to_time
  assign admin = '{}' | parse_json | hash_merge: name: 'owner', l_id: context.params.user_id, r_id: group.id, l_accepted_at: now, r_accepted_at: now, created_by_id: current_profile.id
  function relation = 'modules/community/commands/relationships/find_or_create', object: admin

  assign redirect_url = context.params.return_to | default: context.headers.HTTP_REFERER
  # platformos-check-disable DeprecatedTag
  include 'modules/community/helpers/redirect_to', url: redirect_url
  # platformos-check-enable DeprecatedTag
%}
