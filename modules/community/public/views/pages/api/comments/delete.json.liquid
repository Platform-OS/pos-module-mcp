---
slug: api/comments
method: delete
---
{% liquid
  function current_profile = 'modules/community/helpers/current_profile'
  # platformos-check-disable DeprecatedTag
  include 'modules/community/helpers/profile_present_or_redirect', profile: current_profile
  # platformos-check-enable DeprecatedTag

  function comment = 'modules/community/queries/comments/find', id: context.params.comment.id, creator_id: null

  # platformos-check-disable DeprecatedTag
  include 'modules/community/helpers/present_or_404', property:  comment.id
  # platformos-check-enable DeprecatedTag
  # platformos-check-disable DeprecatedTag
  include 'modules/user/helpers/can_do_or_unauthorized', entity: comment, do: 'remove', requester: current_profile, access_callback: 'modules/community/can/comments', anonymous_return_to: null, redirect_anonymous_to_login: false, forbidden_partial: null
  # platformos-check-enable DeprecatedTag

  function object = 'modules/community/commands/comments/remove', object: comment
  # if object.valid
  #   assign event_payload = null | hash_merge: actor_id: current_profile.id, comment_id: comment.id, tags: slugged_tags
  #   function _ = 'modules/core/commands/events/publish', type: 'comment_deleted', object: event_payload
  # endif

  render 'modules/community/json/shared/show', object: object
%}
